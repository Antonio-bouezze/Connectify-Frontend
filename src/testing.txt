
 black,#101114,#1a1b1f,#28292d,#37393d,
    #37393d,#28292d,#1a1b1f,#101114,
    #101114,#1a1b1f,#28292d,#37393d,
    #37393d,#28292d,#1a1b1f,#101114,black//coloring background part 2




    ///////////
    appp.jsx old file
    import { useState } from "react";
import "./App.css";
import HomePage from "./HomePage";
import ChatApp from "./ChatApp";

function App() {
  const [currentPage, setCurrentPage] = useState("home");

  return (
    <>
      {currentPage === "home" && <HomePage onNavigate={setCurrentPage} />}
      {currentPage === "signin" && <ChatApp onBack={() => setCurrentPage("home")} />}
      {currentPage === "about" && (
         <div className="about-page">
             <div className="about-card">
              <button className="nav-style-btn about-back-btn" onClick={() => setCurrentPage("home")}>
                Back
              </button>
              <h1 className="about-title">About Connectify</h1>
              <p className="about-intro">
               Connectify is a modern communication platform designed to make conversations
               simple, engaging, and accessible. Built around the idea of seamless interaction,
               it offers a space where users can connect, communicate, and build meaningful exchanges
               in a smooth and intuitive environment.
              </p>
               
             <div className="about-section">
              <h2>Our Mission</h2>
              <p>
               Our mission is to create a communication experience that feels natural,
               reliable, and enjoyable. Connectify is built to bring people closer through
               a clean interface, fast interaction, and a user-centered design philosophy.
              </p>
             </div>

             <div className="about-section">
              <h2>What Connectify Offers</h2>
               <ul className="about-features">
                <li>Real-time and intuitive messaging experience</li>
                <li>Clean and modern user interface</li>
                <li>Structured user identity and profile setup</li>
                <li>Scalable design prepared for future room-based communication</li>
               </ul>
             </div>

             <div className="about-section">
              <h2>Project Vision</h2>
              <p>
               Connectify aims to evolve beyond a simple chat application into a complete
               communication platform, with support for richer interactions, stronger user identity,
               and more dynamic ways to connect in the future.
              </p>
             </div>
           </div>
  </div>
)}
      {currentPage === "create-room" && (
        <div className="simple-page">
          <button className="back-btn" onClick={() => setCurrentPage("home")}>
            Back
          </button>
          <h1>Create Room</h1>
          <p>This feature is still under progress.</p>
        </div>
      )}
    </>
  );
}

export default App;




//////////////////////////////




function HomePage({ onNavigate }) {
  return (
    <div className="home-page">
      <header className="top-bar">
        <div className="top-bar-buttons">

            <div className="nav-btn-wrap">
                <button onClick={() => onNavigate("signin")}>Sign-In</button>
            </div>

            <div className="nav-btn-wrap">
                <button onClick={() => onNavigate("about")}>About Us</button>
            </div>

            <div className="nav-btn-wrap">
                <button onClick={() => onNavigate("create-room")}>Create Room</button>
            </div>

        </div>
      </header>

      <main className="hero-section">

        <h1 className="app-title">CONNECTIFY</h1>

        <p>
  Connect instantly, share ideas effortlessly, and stay in control of your conversations. 
  Designed for smooth real-time messaging with a clean and intuitive experience, 
  CONNECTIFY brings communication closer, faster, and more engaging.
       </p>
       
       <div className="features">
        <span>⚡ Real-time messaging</span>
        <span>🔒 Secure communication</span>
        <span>🌐 Scalable chat rooms</span>
       </div> 


        <div className="hero-actions">

          <button className="primary-btn" onClick={() => onNavigate("signin")}>
            Get Started
          </button>

          <button className="secondary-btn" onClick={() => onNavigate("about")}>
            Learn More
          </button>

        </div>
      </main>
    </div>
  );
}

export default HomePage;











//////////////////////////////////////////////////////////









html, body, #root {
  margin: 0;
  min-height: 100%;
}
body {
  margin: 0;
  font-family: Arial, sans-serif;
  color: white;
}
* {
  box-sizing: border-box;
}

/*styling for the home page*/

.app-title {
  font-size: 64px;
  font-weight: 900;
  letter-spacing: 2px;
  position: relative;
  background: linear-gradient(
    10deg,
    #a5b4fc,
    #38bdf8,
    rgb(162, 94, 214),
    rgb(162, 94, 214),
    #38bdf8,
    #a5b4fc
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow:
    0 0 6px rgba(56, 189, 248, 0.25),
    0 0 12px rgba(168, 85, 247, 0.2),
    0 2px 10px rgba(0, 0, 0, 0.6);
  margin-bottom: 16px;
  background-size: 300% auto;
  animation: gradientFlow 3.5s linear infinite;
}
.app-title::after {
  content: "CONNECTIFY";
  position: absolute;
  left: 0;
  top: 0;
  z-index: -1;
  color: transparent;
  -webkit-text-stroke: 1px rgba(255, 255, 255, 0.08);
  transform: translate(3px, 3px);
}
.home-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;

  background: linear-gradient(
    -90deg,
     black,#101114,#1a1b1f,#1a1b1f,#101114,black
  );
  background-size: 400% 400%;
  animation: waveBackground 12s ease-in-out infinite;
}
.top-bar {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  padding: 20px 40px;
  min-height: 80px;
  
}
.top-bar-buttons {
  display: flex;
  gap: 14px;
  align-items: center;
}
.nav-btn-wrap {
  width: 120px;
  height: 33px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.top-bar-buttons button {
  width: 100%;
  height: 100%;
  background: lightgray;
  color: black;
  border: none;
  border-radius: 20px;
  cursor: pointer;
  font-weight: 650;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.top-bar-buttons button:hover {
  color: #ddd;
  background-color: black;
  transform: scale(1.1);
  box-shadow: 0 0 20px darkgray;
  border: 1px solid #ddd;
}
.hero-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  align-items: center;
  padding-top: 80spx;
  text-align: center;
  animation: fadeIn 1s ease forwards;
}
.hero-section p {
  font-size: 18px;
  max-width: 620px;
  line-height: 1.7;
  margin-bottom: 32px;
  color: #cbd5e1;
  letter-spacing: 0.3px;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
}
.hero-actions {
  display: flex;
  gap: 16px;
  align-items: center;
}
.hero-actions button{
  width: 100%;
  width: 120px;
  height: 100%;
  height: 33px;
  background: lightgray;
  color: black;
  border: none;
  border-radius: 20px;
  cursor: pointer;
  font-weight: 650;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.hero-actions button:hover{
  color: #ddd;
  background-color: black;
  transform: scale(1.1);
  box-shadow: 0 0 20px darkgray;
  border: 1px solid #ddd;
}
.features {
  display: flex;
  gap: 20px;
  margin-bottom: 30px;
  color: #9ca3af;
  font-size: 14px;
}

/*styling for the sign-informm*/

.login-page {
  padding: 40px 20px;
  min-height: 100vh;
  display: flex;
  flex-direction: column;

  background: linear-gradient(
    -90deg,
     black,#101114,#1a1b1f,#1a1b1f,#101114,black
  );
  background-size: 400% 400%;
  animation: waveBackground 12s ease-in-out infinite;
}
.login-card {
  width: 100%;
  max-width: 850px;
  background: rgba(20, 20, 24, 0.7);
  border: 1px solid rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px);
  border-radius: 22px;
  padding: 36px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.35);
}
.login-title {
  font-size: 34px;
  margin-bottom: 10px;
  color: #f5f7fb;
  text-align: center;
}
.login-subtitle {
  text-align: center;
  color: #b8bfcc;
  margin-bottom: 28px;
  font-size: 16px;
  line-height: 1.6;
}
.login-form {
  display: flex;
  flex-direction: column;
  gap: 18px;
}
.form-row {
  display: flex;
  gap: 20px;
}
.input-group {
  display: flex;
  flex-direction: column;
  flex: 1;
}
.input-group label {
  margin-bottom: 8px;
  color: #d8deea;
  font-size: 14px;
  font-weight: 600;
}
.input-group input {
  height: 48px;
  padding: 0 14px;
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.12);
  background: rgba(255, 255, 255, 0.06);
  color: #ffffff;
  outline: none;
  font-size: 15px;
  transition: border 0.25s ease, box-shadow 0.25s ease, transform 0.25s ease;
}
.input-group input:focus {
  border-color: #6ea8ff;
  box-shadow: 0 0 0 3px rgba(110, 168, 255, 0.18);
}
.input-group input::placeholder {
  color: #9aa4b2;
}
.error-text {
  margin-top: 7px;
  color: #ff8b8b;
  font-size: 13px;
}
.username-rules {
  background: rgba(255, 255, 255, 0.05);
  border-left: 4px solid #6ea8ff;
  border-radius: 12px;
  padding: 14px 16px;
  color: #c7cfdb;
}
.username-rules p {
  margin: 0 0 8px 0;
  font-weight: 700;
  color: #eef2f7;
}
.username-rules ul {
  margin: 0;
  padding-left: 18px;
}
.username-rules li {
  margin-bottom: 4px;
}
.submit-btn {
  margin-top: 10px;
  height: 50px;
  border: none;
  border-radius: 14px;
  background: linear-gradient(90deg, #4d7cff, #7c5cff);
  color: white;
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  transition: transform 0.25s ease, box-shadow 0.25s ease, opacity 0.25s ease;
}
.submit-btn:hover {
  transform: scale(1.02);
  box-shadow: 0 10px 30px rgba(93, 107, 255, 0.3);
}
.nav-style-btn {
  width: 100%;
  width: 120px;
  height: 100%;
  height: 33px;
  background: lightgray;
  color: black;
  border: none;
  border-radius: 20px;
  cursor: pointer;
  font-weight: 650;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.nav-style-btn:hover{
  color: #ddd;
  background-color: black;
  transform: scale(1.1);
  box-shadow: 0 0 20px darkgray;
  border: 1px solid #ddd;
}

/*styling for about us*/

.about-page {
  padding: 40px 20px;
  min-height: 100vh;
  display: flex;
  flex-direction: column;

  background: linear-gradient(
    -90deg,
     black,#101114,#1a1b1f,#1a1b1f,#101114,black
  );
  background-size: 400% 400%;
  animation: waveBackground 12s ease-in-out infinite;
}

.about-card {
  width: 100%;
  max-width: 950px;
  background: rgba(18, 18, 22, 0.72);
  border: 1px solid rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border-radius: 26px;
  padding: 38px 36px;
  box-shadow:
    0 20px 60px rgba(0, 0, 0, 0.45),
    0 0 0 1px rgba(255, 255, 255, 0.03) inset;
  color: #f5f7fb;
}

.about-back-btn {
  margin-bottom: 24px;
}

.about-title {
  font-size: 40px;
  text-align: center;
  margin-bottom: 20px;
  color: #ffffff;
}

.about-intro {
  font-size: 18px;
  line-height: 1.8;
  text-align: center;
  color: #cbd5e1;
  max-width: 780px;
  margin: 0 auto 30px auto;
}

.about-section {
  margin-bottom: 28px;
}

.about-section h2 {
  font-size: 24px;
  margin-bottom: 12px;
  color: #ffffff;
}

.about-section p {
  font-size: 16px;
  line-height: 1.8;
  color: #c7cfdb;
}

.about-features {
  margin: 0;
  padding-left: 22px;
  color: #c7cfdb;
}

.about-features li {
  margin-bottom: 10px;
  line-height: 1.7;
}
/*styling for message input*/





@media (max-width: 768px) {
  .form-row{
    flex-direction: column;
  }
  

  .login-card {
    padding: 24px;
  }

  .login-title {
    font-size: 28px;
  }
}
@keyframes waveBackground {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}
@keyframes gradientFlow {
  0% {
    background-position: 0% center;
  }
  100% {
    background-position: 300% center;
  }
}
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}